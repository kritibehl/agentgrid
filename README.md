# AgentGrid

**Production-style agent orchestration with retrieval, tools, evaluation gates, and human review.**

`Python` · `FastAPI` · `React` · `TypeScript` · `Redis` · `Prometheus`

🔗 **[Live Demo](https://agentgrid-seven.vercel.app)**

---

## Why This Exists

**Problem:** Production GenAI systems fail in ways that are hard to observe: retrieval misses the relevant document, a tool call silently returns a stale result, an answer is generated without grounding. Aggregate pass rates hide these gaps.

**Impact:** AI systems that look correct in demos fail on real queries. Without eval gates and escalation routing, bad outputs reach users with no mechanism for detection or review.

**Proof:** Full orchestration pipeline with per-stage metrics, eval gates, and human review routing. Retrieval hit rate 80%, tool success rate 80%, p95 latency 880ms — all measured and gated per run.

---

## Quick Links

- Latest report: [reports/latest/agent_reliability_scorecard.json](https://github.com/kritibehl/agentgrid-demo/blob/main/reports/latest/agent_reliability_scorecard.json)
- Tests: `make test`
- Demo: `make demo`
- Live: [agentgrid-seven.vercel.app](https://agentgrid-seven.vercel.app)

---

## Latest Project Output

```json
{
  "retrieval_hit_rate": 0.80,
  "tool_success_rate": 0.80,
  "p95_latency_ms": 880,
  "human_review_gates": "enabled",
  "release_decision": "HOLD"
}
```

*Generated by eval suite. Source: [reports/latest/agent_reliability_scorecard.json](https://github.com/kritibehl/agentgrid-demo/blob/main/reports/latest/agent_reliability_scorecard.json)*

---

## Live Metrics Snapshot

| Signal | Value |
|---|---|
| Retrieval Hit Rate | **80%** |
| Tool Success Rate | **80%** |
| P95 Latency | **880ms** |
| Human Review Gates | **Enabled** |

*Generated from the latest report artifact in `reports/latest/`.*

---

## What This Proves

```
✓ AI Systems
✓ RAG
✓ Agent Orchestration
✓ Evaluation
```

---

## Agent Reliability Scorecard

| Eval Dimension | Coverage |
|---|---|
| Retrieval quality | RAG over docs / logs / runbooks |
| Tool execution | MCP-style tool server · success/failure rates |
| Latency | p95 per workflow stage |
| Cost | Per-run cost tracking |
| Drift detection | Model version regression signals |
| Escalation routing | Human review gates · reviewer workflows |
| Guardrails | Output quality gates · HOLD / SHIP / ESCALATE |

*Scorecard regenerated on every eval run.*

---

## System Flow

```
User query
      │
      ▼
Triage agent
      │
      ▼
Retrieval agent  →  RAG over docs / logs / runbooks
      │
      ▼
MCP tool execution
      │
      ▼
Answer generation
      │
      ▼
Evaluation gate  →  SHIP / HOLD / ESCALATE
      │
      ▼
Human review (if escalated)  →  reviewer workflows · audit logs
```

---

## Architecture

```
AgentGrid/
  Orchestration layer   triage → retrieval → tools → generation
  Eval warehouse        retrieval quality · tool metrics · latency · cost
  Escalation router     HOLD / SHIP / ESCALATE + human review gates
  Observability         trace IDs · audit logs · Prometheus metrics
  Frontend              React/TypeScript dashboard + trace viewer
```

---

## Repository Structure

```
agentgrid-demo/
├── agents/       Triage · retrieval · generation agents
├── tools/        MCP-style tool server
├── rag/          Retrieval over docs / logs / runbooks
├── eval/         Evaluation gates · scorecard · drift detection
├── escalation/   Human review routing · reviewer workflows
├── frontend/     React/TypeScript dashboard + trace viewer
├── observability/ Prometheus metrics · trace exports · audit logs
└── reports/      Eval outputs (CI-generated)
```

---

## Run Locally

```bash
git clone https://github.com/kritibehl/agentgrid-demo && cd agentgrid-demo
pip install -r requirements.txt
make demo    # full agent workflow run
make eval    # eval suite → reports/latest/agent_reliability_scorecard.json
make test    # 57 tests
```

---

## Tests

```bash
make test
# → 57 tests passing
# Retrieval hit rate: 0.80
# Tool success rate: 0.80
# p95 latency: 880ms
```

---

## Selected Outputs

**Eval gate decision:**

```json
{
  "retrieval_hit_rate": 0.80,
  "tool_success_rate": 0.80,
  "p95_latency_ms": 880,
  "cost_per_run_avg": 0.0132,
  "release_decision": "HOLD",
  "hold_reason": "quality risk on entity search scenario"
}
```


## Agent Version Benchmarking

AgentGrid includes agent-version benchmarking for RAG workflow releases:

- agent_v1 vs agent_v2
- retrieval hit rate
- tool success rate
- p95 latency
- cost per run
- eval pass rate
- release decision

See:
- `agent_version_benchmarking/`


## MCP Gateway Infrastructure

AgentGrid includes an MCP-style gateway layer for AI agents and workflow automation:

- tool registry
- tool health report
- MCP call trace
- fleet-style query tool
- workflow trigger tool
- incident lookup tool

See:
- `mcp_gateway/`


## Analytics Warehouse

AgentGrid includes an analytics warehouse layer for AI workflow reporting:

- warehouse tables for runs, users, tools, retrieval events, eval scores, and tool calls
- SQL reports for DAU/WAU-style usage, failure rate, latency by workflow version, retrieval quality, and eval quality
- generated CSV and HTML dashboard reports
- data quality checks for nulls, freshness, and schema validity

See:
- `analytics_warehouse/`
