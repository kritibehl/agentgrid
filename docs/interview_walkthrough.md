# AgentGrid Interview Walkthrough

## 60 seconds
AgentGrid is a production-style AI workflow platform with retrieval, tools, eval gates, and human review.

## 3 minutes
It tracks retrieval hit rate, tool success, p95 latency, cost, drift, and release decisions.

## 10 minutes
Walk through user query → triage → retrieval → tool execution → generation → eval gate → HOLD/SHIP/ESCALATE.
