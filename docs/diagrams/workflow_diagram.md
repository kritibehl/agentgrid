# Workflow Diagram

Customer issue
→ retrieve docs/logs/runbooks
→ tool execution
→ generate answer/action
→ eval gate checks grounding, unsupported detail, retrieval quality, latency budget
→ SHIP / HOLD / ESCALATE
→ incident analytics + support workflow
